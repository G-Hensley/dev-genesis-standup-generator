#===============================================================================
# CI Workflow Template
#===============================================================================
#
# This is a TEMPLATE for your CI/CD pipeline. It's commented out by default
# because it requires customization for your specific tech stack.
#
# HOW TO USE:
# 1. Uncomment the sections relevant to your project
# 2. Adjust commands for your package manager (npm, yarn, pnpm, etc.)
# 3. Update Node version or add other language runtimes as needed
# 4. Add your test and build commands
#
# EXAMPLES INCLUDED:
# - Node.js (npm/yarn/pnpm)
# - Python
# - Go
# - Rust
#
# After customizing, add the job names to .github/settings.yml under
# required_status_checks to enforce them on PRs.
#
#===============================================================================

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Uncomment to enable concurrency control (cancels outdated runs)
# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

jobs:
  #=============================================================================
  # NODE.JS TEMPLATE
  #=============================================================================
  # Uncomment this section for JavaScript/TypeScript projects

  # build-and-test:
  #   name: Build & Test
  #   runs-on: ubuntu-latest
  #
  #   strategy:
  #     matrix:
  #       node-version: [18.x, 20.x]
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js ${{ matrix.node-version }}
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ matrix.node-version }}
  #         cache: 'npm'  # or 'yarn' or 'pnpm'
  #
  #     - name: Install dependencies
  #       run: npm ci  # or: yarn install --frozen-lockfile / pnpm install --frozen-lockfile
  #
  #     - name: Run linter
  #       run: npm run lint
  #
  #     - name: Run type check
  #       run: npm run typecheck  # if using TypeScript
  #
  #     - name: Run tests
  #       run: npm test
  #
  #     - name: Build
  #       run: npm run build
  #
  #     - name: Upload coverage (optional)
  #       uses: codecov/codecov-action@v3
  #       with:
  #         fail_ci_if_error: false

  #=============================================================================
  # PYTHON TEMPLATE
  #=============================================================================
  # Uncomment this section for Python projects

  # build-and-test:
  #   name: Build & Test
  #   runs-on: ubuntu-latest
  #
  #   strategy:
  #     matrix:
  #       python-version: ['3.10', '3.11', '3.12']
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Python ${{ matrix.python-version }}
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: ${{ matrix.python-version }}
  #         cache: 'pip'  # or 'poetry'
  #
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt
  #         pip install -r requirements-dev.txt  # if you have dev dependencies
  #
  #     - name: Run linter (ruff)
  #       run: ruff check .
  #
  #     - name: Run type check (mypy)
  #       run: mypy .
  #
  #     - name: Run tests
  #       run: pytest --cov=. --cov-report=xml
  #
  #     - name: Upload coverage
  #       uses: codecov/codecov-action@v3

  #=============================================================================
  # GO TEMPLATE
  #=============================================================================
  # Uncomment this section for Go projects

  # build-and-test:
  #   name: Build & Test
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Go
  #       uses: actions/setup-go@v5
  #       with:
  #         go-version: '1.22'
  #
  #     - name: Run linter (golangci-lint)
  #       uses: golangci/golangci-lint-action@v4
  #
  #     - name: Run tests
  #       run: go test -v -race -coverprofile=coverage.out ./...
  #
  #     - name: Build
  #       run: go build -v ./...

  #=============================================================================
  # RUST TEMPLATE
  #=============================================================================
  # Uncomment this section for Rust projects

  # build-and-test:
  #   name: Build & Test
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Rust
  #       uses: dtolnay/rust-toolchain@stable
  #       with:
  #         components: clippy, rustfmt
  #
  #     - name: Cache cargo
  #       uses: Swatinem/rust-cache@v2
  #
  #     - name: Check formatting
  #       run: cargo fmt --all -- --check
  #
  #     - name: Run clippy
  #       run: cargo clippy --all-targets --all-features -- -D warnings
  #
  #     - name: Run tests
  #       run: cargo test --all-features
  #
  #     - name: Build
  #       run: cargo build --release

  #=============================================================================
  # PLACEHOLDER JOB
  #=============================================================================
  # This placeholder ensures the workflow file is valid.
  # Remove this once you uncomment one of the templates above.

  placeholder:
    name: CI Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "================================================"
          echo "  CI workflow template - customize for your project!"
          echo "  Edit .github/workflows/ci.yml to enable CI."
          echo "================================================"
          echo ""
          echo "Available templates:"
          echo "  - Node.js (npm/yarn/pnpm)"
          echo "  - Python"
          echo "  - Go"
          echo "  - Rust"
          echo ""
          echo "Uncomment the relevant section and remove this placeholder."
